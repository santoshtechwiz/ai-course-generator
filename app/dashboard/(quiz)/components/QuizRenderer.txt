// import { BlanksQuiz } from '@/lib/quiz/BlanksQuiz';
// import { CodeQuiz } from '@/lib/quiz/CodeQuiz';
// import { MCQQuiz } from '@/lib/quiz/MCQQuiz';
// import { RootState } from '@/store';
// import React from 'react';
// import { useSelector } from 'react-redux';
// import OpenEndedQuiz from '../openended/components/OpenEndedQuiz';

// interface QuizRendererProps {
//   quizType: 'mcq' | 'code' | 'blanks' | 'openended';
//   quizId: string;
// }

// export const QuizRenderer: React.FC<QuizRendererProps> = ({ quizType, quizId }) => {
//   // Get quiz data from Redux store
//   const questions = useSelector((state: RootState) => state.quiz.questions);
//   const currentQuestionIndex = useSelector((state: RootState) => state.quiz.currentQuestionIndex);
//   const answers = useSelector((state: RootState) => state.quiz.answers);
  
//   // If no questions are loaded, show loading state
//   if (!questions || questions.length === 0) {
//     return <div className="py-8 text-center">Loading questions...</div>;
//   }
  
//   // Render the appropriate quiz component based on quiz type
//   switch (quizType) {
//     case 'mcq':
//       return (
//         <MCQQuiz 
//           questions={questions}
//           currentQuestionIndex={currentQuestionIndex}
//           answers={answers}
//           quizId={quizId}
//         />
//       );
      
//     case 'code':
//       return (
//         <CodeQuiz 
//           questions={questions}
//           currentQuestionIndex={currentQuestionIndex}
//           answers={answers}
//           quizId={quizId}
//         />
//       );
      
//     case 'blanks':
//       return (
//         <BlanksQuiz 
//           questions={questions}
//           currentQuestionIndex={currentQuestionIndex}
//           answers={answers}
//           quizId={quizId}
//         />
//       );
      
//     case 'openended':
//       return (
//         <OpenEndedQuiz 
//           questions={questions}
//           currentQuestionIndex={currentQuestionIndex}
//           answers={answers}
//           quizId={quizId}
//         />
//       );
      
//     default:
//       return (
//         <div className="py-8 text-center text-red-600">
//           Unknown quiz type: {quizType}
//         </div>
//       );
//   }
// };
