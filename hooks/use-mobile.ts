/**
 * @deprecated Use `useResponsive` from '@/hooks/use-responsive' instead
 */

import { useResponsive } from './use-responsive';

export default useResponsive;
export const useMobile = useResponsive;
